{
  "name": "portfolioReact",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build" : "cd portfolio && npm run build",
    "install-client" : "cd portfolio && npm install",
    "heroku-postbuild" : "npm run install-client && npm run build",
    "start": "node server.js",
    "client": "cd portfolio && npm start",
    "dev": "concurrently -n 'server,portfolio' -c 'yellow,green' \"nodemon server.js\" \"npm run client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "bootstrap": "^5.1.3",
    "concurrently": "^6.4.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.12",
    "morgan": "^1.10.0",
    "react-bootstrap": "^2.0.2",
    "react-icons": "^4.3.1",
    "react-router-dom": "^6.0.2"
  }
}
